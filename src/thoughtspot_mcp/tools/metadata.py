"""Metadata tools for the ThoughtSpot API (Wave A — task T1.1).

Planned tools:
    - thoughtspot_search_metadata: multi-strategy search over POST /metadata/search
      (type -> name_pattern -> unfiltered; handle MODEL / WORKSHEET / LOGICAL_TABLE
      type quirks).
    - thoughtspot_delete_metadata: POST /metadata/delete (destructiveHint=True).

This module is an intentional empty stub registered in ``tools/__init__`` so that
the worktree implementing T1.1 owns this file exclusively. Do not add shared
infrastructure here.
"""

from __future__ import annotations

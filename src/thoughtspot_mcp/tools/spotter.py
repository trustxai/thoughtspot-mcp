"""Spotter / AI tools for the ThoughtSpot API (Wave B — task T1.9, Beta).

Planned tools (all Beta — mark clearly in docstrings):
    - ai conversation: POST /ai/conversation/create + /ai/conversation/{id}/converse
    - relevant questions: POST /ai/relevant-questions/  (NOTE the real trailing slash!)
    - datasource suggestions: POST /ai/data-source-suggestions

Follow-up (later wave): the newer ai/agent/* family (Spotter 3 session tools).

Intentional empty stub registered in ``tools/__init__``; owned by the T1.9 worktree.
"""

from __future__ import annotations

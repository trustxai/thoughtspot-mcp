"""Data-query tools for the ThoughtSpot API (Wave A — task T1.4).

Planned tools:
    - thoughtspot_search_data: POST /searchdata (TML search syntax).
    - thoughtspot_get_liveboard_data: POST /metadata/liveboard/data.
    - thoughtspot_get_answer_data: POST /metadata/answer/data.
    - Truncate large result sets to protect the LLM context window
      (see formatters.truncate_records).

Intentional empty stub registered in ``tools/__init__``; owned by the T1.4 worktree.
"""

from __future__ import annotations
